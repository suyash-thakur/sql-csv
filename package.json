{
  "name": "csv-sql",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "csv-parser": "^3.0.0"
  },
  "devDependencies": {
    "tap": "^16.3.7"
  },
  "directories": {
    "lib": "lib"
  },
  "description": "This is a simple csv to sql converter that takes a csv file and allows you to run sql queries on it"
}
